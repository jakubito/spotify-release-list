/**
 * Render automation settings screen
 *
 * @param {RouteComponentProps} props
 */
function AutomationSettings(props) {
  return <div className="fade-in">TODO</div>
}

export default AutomationSettings
